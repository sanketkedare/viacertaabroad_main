import React from 'react'

const ForMBBS = ({setIsOpenDropdown}) => {
  return
  return (
    <div    className="absolute min-h-[300px] w-[800px] bg-white top-14 left-4/12 z-30 rounded-xl inset-0  backdrop-blur-3xl bg-opacity-50 p-6 flex items-center gap-5 flex-wrap"
    onClick={() => setIsOpenDropdown(false)}>
      For MBBS
    </div>
  )
}

export default ForMBBS
